use std::thread;

struct Log {
    id: u64,
    text: String
}

struct Account {
    balance: u64,
    logs: Vec<Log>
}

fn perform_transaction(source_account: u64, destination_account: u64, sum: u64) {

}

fn main() {
    println!("Hello 🦀s! ");  
    
    let next_log_id = 1;
    const NUMBER_OF_THREADS = 10; 

    for i in 
    thread_handle = thread::spawn(move || {
        
    })
}
